# Teste Prático ReactJS

## Getting Started

1.  Instale as dependências do projeto utilizando o `yarn` ou o seu gerenciador de pacotes preferido.
2. Rode o comando `yarn start` para iniciar o servidor.
